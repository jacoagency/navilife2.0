/** @type {import('next').NextConfig} */
const nextConfig = {
    // Las Server Actions ahora están disponibles por defecto
    images: {
        domains: ['fal.media'],
    },
};

module.exports = nextConfig;
